# ShopNotes
This project helps a customer create their shopping list. The shopping list is then stored as a text file that can be printed or stored as a text file on either a phone or a laptop. The language used for this project is Java.
